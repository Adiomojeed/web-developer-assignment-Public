export type UserData = {
  id?: string;
  name: string,
  email: string,
  street: string,
  state: string,
  city: string,
  zipcode: string
};

export type PostData = {
  id?: string;
  title: string,
  body: string,
};